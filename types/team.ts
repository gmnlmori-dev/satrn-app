export type TeamSummary = {
  id: string;
  name: string;
  slug: string;
  isActive: boolean;
  createdAt: string;
  updatedAt: string;
};

export type TeamSelectOption = {
  id: string;
  name: string;
  slug: string;
};
